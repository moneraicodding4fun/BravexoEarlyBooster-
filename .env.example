# Bravexo EarlyBooster — environment
#
# Leave both values empty to run in Demo Mode (rich mock data, fully
# functional UI). Fill them in to go live against your Supabase project.

VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Port for the universal relay (server/index.js)
PORT=8080
